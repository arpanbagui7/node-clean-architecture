Create .env and add DATABASE_URL
For e.x -
DATABASE_URL="mysql://root:password@localhost:3306/testdb?schema=public"

#Command to run

1. npm start -- to start typescript and create dist folder
2. npm run dev -- to start node server
3. npm run test -- to run test
